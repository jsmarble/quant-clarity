# Design documentation

This directory contains implementation decisions derived from the approved product requirements. It must not redefine product goals or silently weaken requirements.

## Primary artifact

[`system-design.md`](system-design.md) is the approved design baseline. Product-owner approval and the zero-visitor-data amendment were recorded on 2026-08-01; implementation evidence remains separate.

The current model/variant exact-name follow-up is split by [ADR 0026](../decisions/0026-blob-model-variant-exact-search-cutover.md). [Phase 5G-A1](phase-5g-a1-model-variant-durable-proof-core.md) implements only the runtime-neutral storage-row, revision-bound staging, and dormant artifact-proof core. [Phase 5G-A2](phase-5g-a2-model-variant-search-cutover.md) locally implements the atomic D1/v3 cutover, capacity and queryability evidence, and local restore-rebuild boundary. [Phase 5G-B](phase-5g-b-model-variant-exact-reader.md) locally implements the bounded canonical reader, tier-local continuation, bookmark-continuous local RPC, internal API seam, and exact acceptance evidence. It still creates no public route or merged cursor. [ADR 0027](../decisions/0027-trusted-provider-model-id-projection.md) and [Phase 5H-A1](phase-5h-a1-provider-model-id-projection.md) define the locally implemented runtime-neutral provider-model-ID derivation. [ADR 0028](../decisions/0028-provider-model-id-durable-storage-cutover.md) and [Phase 5H-A2](phase-5h-a2-provider-model-id-storage.md) locally implement the dual-BLOB schema, bounded writer, v4 proof cutover, and restore-rebuild boundary. [ADR 0029](../decisions/0029-provider-model-id-exact-reader.md) and [Phase 5H-B1](phase-5h-b1-provider-model-id-reader.md) locally implement the literal raw-first provider-ID reader, canonical target mapping, same-witness filters, neutral tier-local order, fourth local RPC method, and internal API seam. [ADR 0030](../decisions/0030-composed-exact-search-and-compact-cursor.md) and [Phase 5H-B2](phase-5h-b2-composed-exact-search.md) define the local multi-tier exact composer, fixed compact continuation, authenticated cursor seam, and provider-only semantic-applicability state. [ADR 0031](../decisions/0031-retained-hot-publication-continuity.md) and [Phase 5I-A](phase-5i-a-retained-hot-publication-continuity.md) locally implement the schema-`1.8.0`, indexed immutable-history, horizon-aware V2 continuity boundary across multiple switches. [ADR 0032](../decisions/0032-local-named-query-service-binding.md) and [Phase 5I-B](phase-5i-b-local-named-query-service-binding.md) configure and prove the actual local API-to-query named-entrypoint JSRPC binding without changing the public handler or binding D1 to the API. [ADR 0033](../decisions/0033-provider-eligibility-filtering.md) and [Phase 5J](phase-5j-provider-eligibility-filter.md) define the schema-`1.9.0` indexed target-eligibility and composed stable-provider-ID filter boundary without provider ranking or public routing. [ADR 0034](../decisions/0034-canonical-family-filtering.md) and locally implemented [Phase 5K](phase-5k-canonical-family-filter.md) define the schema-neutral canonical family filter across both exact Model/Variant target tiers. [ADR 0035](../decisions/0035-canonical-family-model-variant-publication-closure.md) and locally implemented [Phase 5L](phase-5l-canonical-publication-relationship-closure.md) define the bounded pre-release hard cutover that rejects orphaned or contradictory ModelFamily/Model/Variant relationships at the common persisted-content manifest boundary. [ADR 0036](../decisions/0036-publication-pinned-dataset-metadata.md) and [Phase 5M](phase-5m-public-dataset-metadata.md) define the first locally routed data representation: publication-pinned metadata over V2, one fixed SELECT-only query, corrected precision-policy versions, strong ETags, and zero visitor storage. [ADR 0037](../decisions/0037-stale-offering-eligibility-filtering.md) and locally implemented [Phase 5N](phase-5n-stale-offering-filter.md) add the schema-`1.11.0` explicit stale-Offering filter boundary without opening `/v1/search`. Prefix/keyword, semantic, the separate model and remaining offering-derived filters, every other public data route, remote resources and production/preview bindings, deployment, operational restore, physical pruning, complete search, and release evidence remain pending.

[ADR 0049](../decisions/0049-public-methodology-detail.md) and [Phase 5Q](phase-5q-public-methodology-detail.md) accept a local/test-only API slice: registered GET/HEAD use resolver V2 then one dedicated bookmark-continuous SELECT-only publication-context read, while registry/environment closure withholds unregistered and preview/production `404` responses until after limiting and prevents downstream reads. Every methodology response is `private, no-store`. [Phase 5T](phase-5t-local-methodology-acceptance.md) supplies the separate local semantic methodology, stable historical page, material-change log, and browser evidence. Neither slice opens a remote route or advances a mapped traceability row; deployed accessibility, historical-retention, and release evidence remain pending.

[ADR 0050](../decisions/0050-gdpr-accountability-readiness.md) and [Phase 5R](phase-5r-gdpr-accountability-readiness.md) define a deterministic pending-only GDPR accountability inventory. The closed manifest hash-binds the working notice and public-safe drafts, requires exact missing-evidence categories, and refuses approval, release, or compliance claims. Its passing local checker proves only continued release blocking; authorized owner records and the deployed-notice hash remain necessary for `GATE-gdpr-accountability`, and every mapped row stays `Planned`.

[ADR 0052](../decisions/0052-publication-pinned-frontend-model-detail.md) and [Phase 5U](phase-5u-publication-pinned-model-facts.md) accept a local-only signed canonical Model Facts slice. The frontend must pin the detail read to its metadata publication, forward no visitor identity, and admit only closed-contract responses from the existing Model-detail authority. Public Model API routing, Variants, offerings, recovery acceptance, remote environments, deployment, and every traceability advancement remain pending.

[ADR 0053](../decisions/0053-publication-pinned-exact-model-discovery.md) and locally implemented [Phase 5V](phase-5v-exact-model-discovery.md) connect publication-pinned exact Model identity results to stable-ID Model Facts. Accepted [ADR 0054](../decisions/0054-canonical-model-card-projection.md) and locally implemented [Phase 5W](phase-5w-exact-model-cards.md) retain canonical Models already read by exact tiers, add zero SQL, and project a dedicated 65,536-byte-bounded Model-card collection whose bytes are invariant to provider/filter witnesses. Variant cards, public/remote ingress, operational `PublicationWorkflow` bindings and execution, deployment, and every traceability advancement remain pending.

Accepted [ADR 0055](../decisions/0055-publication-pinned-exact-variant-cards.md) and locally implemented [Phase 5X](phase-5x-exact-variant-cards.md) apply the same zero-extra-SQL boundary to explicit Variants without inheriting canonical Model facts. The local `/variants` journey visibly differentiates Variants and links their structural canonical Model while keeping Variant detail, provider comparison, public/remote ingress, and deployment closed. Provider comparison follows only after complete Offering/Provider/target/Price/PrecisionObservation/EvidenceSummary relationship closure and an accepted current-value policy.

Accepted [ADR 0056](../decisions/0056-offering-relationship-closure.md) and locally implemented [Phase 5Y-A](phase-5y-a-offering-relationship-closure.md) define the first provider-comparison prerequisite as a persisted-content hard cutover. Before a trusted manifest escapes, the bounded validator proves complete Provider/Offering/Model-or-Variant/Price/PrecisionObservation/EvidenceSummary contracts and identities, exact forward/reverse child sets, exact Offering applicability except component scope, evidence existence and subject ownership, and time ordering. Current-value selection, evidence field/value semantics, comparison projection, API/UI, source access, remote resources, and deployment remain explicitly deferred; every traceability status remains unchanged.

Accepted [ADR 0057](../decisions/0057-selection-free-offering-observation-set.md) and locally implemented [Phase 5Y-B](phase-5y-b-offering-observation-set.md) define the next internal comparison prerequisite without choosing a current fact. The projector reconciles a trusted manifest to the complete exact persisted resource bytes, retains every Price, PrecisionObservation, and applicable EvidenceSummary for each Offering, copies only the Provider display-name Fact, and proves that Fact's evidence has the Provider subject. Stable-ID serialization, explicit aggregate membership counts, per-set hashes, and an inventory hash make the lossless projection deterministic without implying recency or rank. Current-value and conflict policy, comparison rows/order, API/UI, source access, remote resources, and deployment remain deferred; every traceability status remains unchanged.

Accepted [ADR 0063](../decisions/0063-current-offering-observation-authority.md) and [Phase 5Y-C](phase-5y-c-current-offering-observation-authority.md) record the manifest-bound authority missing before current Offering selection. Independent review rejected promotion of extensible public EvidenceSummary source labels into provider ownership, source approval, verification, or precedence. A successor publication-format cutover must bind every Price and PrecisionObservation to fenced canonical claim/source-policy/source-register authority and include that artifact in closure, readiness, backup, and restore before current selection or comparison can begin. No selector or traceability advancement is claimed.

Accepted sequencing [ADR 0064](../decisions/0064-sequence-fenced-claim-authority-cutover.md) and [Phase 5Y-D](phase-5y-d-fenced-claim-authority-sequence.md) prove the legacy graph cannot supply that authority: typed policy order, verification receipts, approved endpoint identity, complete public-field commitments, and provenance-v2 fencing are absent. The next code must implement those inputs first, then one cumulative claim-authority/closure/readiness/recovery cutover coordinated with owner-pending ADR 0045. No codec, selector, public path, or traceability advancement is authorized.

[ADR 0065](../decisions/0065-fenced-provenance-v2-authority.md) and [Phase 5Y-E](phase-5y-e-fenced-provenance-v2-authority.md) fix the implementable v2 boundary: a separately approved provenance authority plan, current-fence-bound immutable Provider bundles, normalized source/policy/verification/claim relationships, and a complete seal. Partial rows, unfenced legacy Offering rows, and the existing run-plan policy hash are not provenance authority. Implementation begins with dormant exact-predecessor shapes whose approval, opening, acquisition, and effect paths are unconditionally blocked.

[Phase 5Y-F](phase-5y-f-dormant-provenance-v2-foundation.md) locally implements that first physical slice in canonical migration 0008. The static capability and empty installation/plan/endpoint/bundle/permit/response shapes are `STRICT`, append-only, and structurally linked; exact-predecessor and legacy-guard checks fail atomically. Every runtime insert and the existing source-backed roster outcome remain unconditionally blocked.

[ADR 0066](../decisions/0066-dormant-provenance-v2-registration.md) and [Phase 5Y-G](phase-5y-g-dormant-provenance-v2-registration.md) define migration 0009's safe successor boundary: normalized source-register, manifest, endpoint-policy, field-policy, verifier-policy, lifecycle, and set-root shapes may install while every migration-0008 runtime blocker remains exact and active. The shapes are proof inputs only; protected registration, independent root recomputation, authority-plan approval, Provider-bundle opening, acquisition, source effects, public paths, and deployment remain blocked.

[Proposed ADR 0067](../decisions/0067-protected-provenance-registration-activation.md) and locally implemented [Phase 5Y-H](phase-5y-h-protected-provenance-registration-activation.md) define the candidate atomic authorization successor without assuming an unbounded D1 transaction. Generated non-public plan/receipt/mapping schemas, field corpus, canonical-byte/root registries, typed digest/count/traversal bindings, semantic policy and independent vectors are review candidates, not normative authority. [Phase 5Y-H1](phase-5y-h1-independent-composite-root-evidence.md) independently composes every reviewed leaf family through the proposed family-projection and higher-frame codec. [Phase 5Y-H2a](phase-5y-h2a-connected-registration-graph.md) freezes the complete 371-row leaf-preimage inventory, [Phase 5Y-H2b](phase-5y-h2b-connected-traversal-vectors.md) independently recomputes its leaves, typed ordering, 386 projections, nine collections and synthetic authority/refused-frame linkage, [Phase 5Y-H2c](phase-5y-h2c-connected-successor-manifest-vectors.md) proves exact synthetic successor-manifest JCS and adapter-receipt parity, [Phase 5Y-H2d-a](phase-5y-h2d-a-canonical-registration-document-resolvers.md) implements strict duplicate-aware canonical-byte admission plus all 45 checked document selector programs independently in Node and workerd, [Phase 5Y-H2d-b](phase-5y-h2d-b-connected-registration-document.md) adds a complete schema-valid synthetic document, all 31 safe-preimage occurrences, 46 scoped document counts and exact retained chunks, and [Phase 5Y-H2d-c](phase-5y-h2d-c-connected-document-cascade.md) overlays those commitments and independently regenerates the successor, four plan roots, final canonical document and refused cascade. External/repository resolvers, document-output/semantic-oracle closure, accepted-scale evidence, migration 0010 and fixed private writers remain pending; `benchmark_pending` is the only admitted aggregate status. Permits, responses, source effects, source-backed outcomes, artifacts, serving, remote resources, and deployment remain blocked.

Accepted [ADR 0058](../decisions/0058-dormant-unbound-publication-workflow-planner.md) and locally implemented [Phase 7.2-A](phase-7-2-a-dormant-publication-workflow-planner.md) export the actual `PublicationWorkflow` class over Wrangler's generated empty `CloudflareEnv` while keeping the pipeline Worker unbound and unscheduled. One exact Monday/Thursday event produces only a sub-4-KiB, non-authoritative occurrence plan through one pure step; the stable environment-neutral logical schedule name is `provider-refresh-v1`, distinct from the preview-only Workflow resource reservation. Direct workerd invocation and an unbound Wrangler dry-run prove local class/config compatibility only, not Workflow-engine persistence, retries, scheduling, or restart. D1/run-plan/provider/status semantics, every binding, remote resource, deployment, and traceability advancement remain pending.

Accepted [ADR 0059](../decisions/0059-normalized-publication-run-plan-authority.md) and locally implemented [Phase 7.2-B](phase-7-2-b-publication-run-plan-authority.md) close only the meaning of a scheduled run plan. Additive canonical migration `0005_publication_run_plan_authority.sql` normalizes immutable plan, Provider, policy, seal, approval, revocation, and capability records; plan membership independently freezes every referenced roster, roster item, and source-compliance authority row. The fixed read-only resolver requires the exact ID/hash, expected environment, expected canonical-schema and pipeline-contract versions, and canonical Monday/Thursday scheduled instant, then recomputes provider-scope, policy-set, and root hashes and checks source approval at that instant without selecting “latest.” Focused migration, resolver-unit, and actual workerd/D1 verification passes. No real plan, ledger write, Workflow change/binding, preview/production migration, deployment, or traceability advancement is authorized; admission, terminality, deadline, concurrency, report, schedule, and replay semantics require the next ADR.

Accepted [ADR 0060](../decisions/0060-publication-run-admission-and-terminal-coordination.md) and [Phase 7.2-C](phase-7-2-c-publication-run-admission-and-terminal-coordination.md) close those coupled semantics in a runtime-neutral contract. Rejected firings create no run; engine retries remain one attempt; protected replays are exact, adjacent, and terminal; deadlines anchor to scheduled time; Provider exclusion spans occurrences and requires monotone fencing; run outcome is separate from `publish_new`, `retain_current`, or `blocked`; and a bounded `publication-run-report@2` contains only closed non-visitor fields. This phase deliberately adds no ledger migration or write adapter because the current source-evidence foreign key cannot truthfully represent pre-acquisition terminal failure. Workflow activation, bindings, remote resources, deployment, and every traceability status remain pending.

Accepted [ADR 0061](../decisions/0061-additive-publication-orchestration-ledger.md) and locally implemented [Phase 7.2-D](phase-7-2-d-publication-orchestration-ledger.md) define a parallel coordination D1 authority rather than overloading or shadow-writing the legacy run/evidence vocabulary. Attempt 1 is the only active admitted receipt, budget reservation exact-compares one allocated global monthly state, Provider ownership requires immutable claim/reconciliation/release history plus a monotone guarded head, and source-free roster failures use operational outcomes instead of invented evidence. Rejection and replay shapes are present but activation is blocked until atomic D1 admission and protected replay resolvers exist; retained publication and source effects are likewise blocked. The Phase C report boundary distinguishes canonical `prn_` Provider slices from `pvr_` Provider runs. Local migration/adapter verification passes; bindings, remote migrations, runtime activation, and deployment remain prohibited.

Accepted [ADR 0062](../decisions/0062-admission-authority-monotonicity.md) and locally implemented [Phase 7.2-D1](phase-7-2-d1-admission-authority-hardening.md) reserve `plan_not_found` for a truly absent run-plan ID and add canonical migration `0007_admitted_plan_revocation_history.sql`, which refuses contradictory predecessor history and prevents late revocation from rewriting an immutable admitted scheduled instant. The generic admission-rejection blocker remains installed; no binding, remote migration, runtime activation, or deployment is authorized.

## Design rules

- Trace each component and consequential behavior to PRD requirement IDs.
- Clearly separate facts, decisions, assumptions, constraints, open questions, and rejected alternatives.
- Record hard-to-reverse or cross-cutting decisions as ADRs under `docs/decisions/`.
- Verify current Cloudflare APIs, limits, pricing, compatibility requirements, and Wrangler configuration before relying on them.
- Implement only accepted decisions and preserve explicit unresolved compatibility or legal gates rather than silently changing product semantics.
- Preserve the PRD's neutrality, evidence, privacy, security, accessibility, publication, and public-access invariants.
