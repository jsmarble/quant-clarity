# QuantClarity implementation and release plan

| Attribute | Value |
|---|---|
| Status | Approved and in progress; product-owner approval recorded 2026-08-01 |
| Authority | Approved PRD, approved system design, and accepted ADRs |
| Traceability | [`traceability.md`](traceability.md), [`verification-plan.md`](verification-plan.md) |
| Scope | Vertical slice through four-provider production release |

This plan is a delivery sequence, not approval to scaffold code, provision resources, or publish data. A phase advances only when its exit evidence exists. Unknown provider facts and failed gates remain explicit; schedule pressure never weakens evidence, neutrality, privacy, accessibility, security, cost, or atomic-publication controls.

## Non-delegable approvals

Agent execution and technical review cannot grant legal, product, spending, or public-release authority. The named authorized owner must record approval before: accepting a source/legal register; selecting code or dataset/API terms; clearing a name/domain; creating paid Cloudflare resources beyond an already accepted budget; enabling a provider in production; deploying production; or making the first public publication. Design approval authorizes implementation only to the extent explicitly stated by the owner; it does not silently grant those later decisions.

## Phase 0 — governance transition

Status: complete on 2026-08-01. The approved privacy amendment added ADR 0011 and its release gates before runtime scaffolding; the approved frontend amendment added ADR 0012. The governance baseline is committed separately from the runtime foundation. Preview, migration, and deployment commands remain intentionally gated until their corresponding resource inventory and protected environments exist.

Trigger: satisfied by explicit product-owner approval of the design and Section 2.4 defaults on 2026-08-01.

1. Record the accepted defaults in `docs/product/decision-log.md` without changing PRD semantics.
2. Mark the system design `Approved`, mark ADRs `Accepted`, and record the approval date/owner.
3. Update `AGENTS.md` and `README.md` from system-design to implementation phase.
4. Add the exact supported setup, format, lint, type-check, unit, integration, build, preview, migration, dry-run, and deployment commands to `AGENTS.md` after the installed toolchain proves them.
5. Commit the approved design separately from runtime scaffolding so the governance boundary is reviewable.

Exit evidence: approved design state is internally consistent, `git diff --check` passes, and no trace row advances beyond `designed`.

## Phase 1 — reproducible foundation and machine contracts

Status: complete on 2026-08-01. PR #1 established the pinned workspace, strict checks, generated contracts, privacy/docs checks, CI, Worker dry-run surfaces, and guarded frontend shell. The Phase 2 branch preserves those gates.

1. Pin Node/npm, create the npm-workspace layout from ADR 0001, install only approved production/development dependencies, and commit one lockfile.
2. Establish strict TypeScript, formatting/linting, deterministic test configuration, dependency/license/secret scanning, and Wrangler 4 JSONC/type generation.
3. Generate versioned JSON Schemas and OpenAPI 3.1 from the approved canonical/API/adapter/publication contracts; examples must validate against the generated schemas.
4. Create environment inventories for local, test, preview, and production with distinct names/IDs and no secret values.
5. Add CI jobs for clean install, static checks, contract generation drift, tests, build, scans, and Cloudflare dry-run validation.

Exit evidence: reproducible clean build, generated-contract diff is empty, preview/production inventory isolation is machine-checked, and `GATE-api-contract` has an executable skeleton without claiming conformance yet.

## Phase 2 — canonical core and first lawful fixture

Status: in progress. Canonical and serving D1 migrations, constraint/property tests, adapter roster/fixture contracts, a production-disabled Fireworks adapter, synthetic non-publishable fixture, pending source register, and publication-pin ADR 0013 exist locally. Real provider acquisition and Phase 2 exit remain blocked on authorized source-register approval.

1. Implement stable IDs, resource registry/type triggers, organizations/publishers, models/variants/checkpoints/parameters, providers/offerings, typed claim scopes, observations/evidence, claims/conflicts, precision components, exact-decimal prices, schedules/runs, policies, and publication metadata.
2. Add D1 migrations plus schema/constraint/property tests for identity, typed applicability, precedence, supersession, null/unknown behavior, decimal ordering, staleness, and neutrality.
3. Prepare the Fireworks source-compliance register and credential/access review for approval by the authorized owner/legal reviewer. If approval is denied, propose the first approvable structured launch candidate without weakening the adapter contract.
4. Retain only minimal legally retainable redacted fixtures with provenance notes; seed malicious, missing, drift, pagination, conflict, price, precision, and base-object applicability cases.
5. Implement the deterministic adapter boundary with no storage access and prove every roster item reaches a terminal result.

Exit evidence: canonical and adapter contract suites pass; the local inputs of `GATE-applicability-integrity`, canonical/fixture portions of `GATE-evidence-dlp`, and provider-fixture portions of `GATE-legal-source-register` have retained partial artifacts. The composite gates remain pending until their preview/other declared inputs and approvals exist.

## Phase 3 — durable acquisition and evidence pipeline

Status: in progress. Runtime-neutral pipeline and acquisition-security kernels now implement locally testable scheduling, idempotency, retry, anomaly, destination, policy, budget, and pre-retention evidence decisions. They perform no I/O. Workflow/D1 integration, live source access, D1/R2 writes, DNS/fetch/browser execution, deployed canaries, and preview resources remain pending their existing approval and environment gates.

1. After explicit budget/resource authorization, provision only the bounded preview frontend/API/query/pipeline Workers, D1, R2, Vectorize, Workflow, limiter, AI, and observability resources required by the vertical slice from checked-in inventory. Apply deletion protection and run preliminary preview/production-identity isolation checks; no production data/resource may exist yet.
2. Implement the directly scheduled parent Workflow and independently identified provider child instances with occurrence/run/attempt identities and bounded idempotent steps.
3. Implement exact-host acquisition, manual redirects, byte/time/page ceilings, DNS defense-in-depth, deployed SSRF canaries, robots/Content Signals policy evaluation, and Browser Sessions only when an approved source requires them.
4. Apply streaming/in-memory minimization, DLP, and redaction before any durable state, hash, log, fixture, or AI input; promote only verified redacted evidence to locked R2 retention.
5. Implement deterministic extraction/validation first, anomaly re-fetch, quarantine, last-known-good carry-forward, cost admission control, and complete machine-readable run reports.
6. Keep generative extraction disabled until a separately accepted Workers AI gold-set policy requires it; external AI remains an ADR amendment and `CF-009` exception.

Exit evidence: restart/retry/idempotency/failure-isolation tests pass; `GATE-source-egress-security`, `GATE-extraction-adversarial`, and pipeline portions of `GATE-evidence-dlp` pass in isolated preview.

## Phase 4 — atomic publication, backup, and search

Status: in progress. A runtime-neutral publication kernel now provides locally testable immutable-closure hashing, provider-slice/search/vector inventory, readiness, activation/rollback, switch-history, backup-manifest, and no-pruning decisions under [ADR 0015](../decisions/0015-publication-closure-and-lifecycle.md). [Phase 4B](phase-4b-serving-dispositions.md) adds local serving-D1 persistence for null unavailable dispositions and stable carried-forward lineage under [ADR 0017](../decisions/0017-provider-disposition-persistence.md). [Phase 4C](phase-4c-sealed-closure.md) and [ADR 0018](../decisions/0018-sealed-serving-closure-persistence.md) add exact persisted closure projection. [Phase 4D1](phase-4d1-readiness-ledger.md) and [ADR 0019](../decisions/0019-seal-bound-readiness-ledger.md) add local publication-scoped FTS, four typed receipt projections, complete-vector evidence fields, one immutable readiness attestation, and a seal-bound `building` to `ready` gate. [Phase 4D2](phase-4d2-exact-generation-switching.md) and [ADR 0020](../decisions/0020-exact-generation-publication-switching.md) add local fresh switch-preflight projections, exact-generation activation/one-step rollback, and append-only switch history without inserting a head. [Phase 4D3](phase-4d3-fixed-d1-switch-adapter.md) adds local fixed prepared-statement switch execution and ambiguous-outcome reconciliation. [Phase 4D4](phase-4d4-fixed-d1-readiness-adapter.md) adds the missing local atomic readiness-commit boundary without a deployable binding. [ADR 0021](../decisions/0021-canonical-provider-exact-search.md) fixes the provider-name projection and neutrality boundary; [Phase 4E](phase-4e-provider-search-core.md) implements its pinned Unicode normalization and complete-manifest-bound trusted provider projection core, [Phase 4F](phase-4f-provider-search-v2-proofs.md) adds isolated v2 readiness and switch-preflight proof primitives, and [Phase 4G](phase-4g-provider-search-schema-writers.md) adds serving schema `1.5.0`, revision-bound pre-seal provider staging, persisted v2 reconstruction, and fixed readiness/switch writers with local SQLite and workerd evidence. Provider query evidence, remote D1 evidence, R2 archive proof, Cloudflare Vectorize visibility, populated-cache chaos, writer-drained backup, isolated restore, physical pruning, preview resources, and all composite gates remain pending.

1. Build immutable serving projections and FTS documents from one validated provider slice.
2. Create exactly one Vectorize vector per model/explicit variant and implement D1 eligibility, bounded semantic batches, deterministic merge, degradation, and provider-count neutrality.
3. Implement candidate readiness probes and the single transactional publication-head switch; API and SSR caches resolve the head before selecting publication-keyed entries.
4. Implement pointer rollback, hot generation retention, canonical writer-drained consistent backup, logical serving export, FTS/Vectorize reconstruction, integrity manifests, and isolated restore.
5. Exercise failure injection at every publication phase and with populated multi-PoP caches.

Exit evidence: local decision evidence is recorded in [`phase-4-local-kernel.md`](phase-4-local-kernel.md), [`phase-4b-serving-dispositions.md`](phase-4b-serving-dispositions.md), [`phase-4c-sealed-closure.md`](phase-4c-sealed-closure.md), [`phase-4d1-readiness-ledger.md`](phase-4d1-readiness-ledger.md), [`phase-4d2-exact-generation-switching.md`](phase-4d2-exact-generation-switching.md), [`phase-4d3-fixed-d1-switch-adapter.md`](phase-4d3-fixed-d1-switch-adapter.md), [`phase-4d4-fixed-d1-readiness-adapter.md`](phase-4d4-fixed-d1-readiness-adapter.md), [`phase-4e-provider-search-core.md`](phase-4e-provider-search-core.md), [`phase-4f-provider-search-v2-proofs.md`](phase-4f-provider-search-v2-proofs.md), and [`phase-4g-provider-search-schema-writers.md`](phase-4g-provider-search-schema-writers.md). The configured and remotely exercised D1 binding plus all remote artifact, reader, backup, and chaos evidence remain pending. `GATE-publication-chaos`, `GATE-search-acceptance`, and `GATE-restore-and-rebuild` still must pass for the deployed vertical slice before Phase 4 exits.

## Phase 5 — public API and bounded edge

Phase 5A first implements the pure, storage-free decision boundary in [ADR 0016](../decisions/0016-bounded-local-api-read-protocol.md): bounded validation before limiter effects, versioned authenticated cursors, closed live-only service envelopes, publication-aware conditional responses, fixed trusted cache origin, injected ceilings, and exact/structured search with public semantic processing disabled. Its scope and non-claims are recorded in [`phase-5a-local-api-kernel.md`](phase-5a-local-api-kernel.md). This local work does not advance any traceability row beyond `Planned` or replace the runtime steps below.

Phase 5B adds the first internal serving-D1 reader: a fixed SELECT-only, bounded, canonical-rehydrating provider exact-name operation over already selected ADR 0013 hot publications. Its local/runtime evidence and remaining public-integration, hot-inventory, NUL/storage, and release gaps are recorded in [`phase-5b-provider-exact-name-reader.md`](phase-5b-provider-exact-name-reader.md). Traceability remains `Planned`.

Phase 5C resolves only that reader's provider-name U+0000 contract/storage mismatch under [ADR 0022](../decisions/0022-forbid-nul-provider-display-names.md): reject NUL in known canonical provider display names and matching exact-name queries, add fail-closed serving schema `1.5.1` insert guards, and leave `exact-search-normalization@1` and all other Unicode behavior unchanged. The boundary, version distinctions, implementation order, and non-claims are recorded in [`phase-5c-provider-name-nul-boundary.md`](phase-5c-provider-name-nul-boundary.md). No deployment or provisioning is part of this slice. Traceability statuses remain unchanged: existing `Implemented` rows stay `Implemented`, and the other mapped rows remain `Planned`.

Phase 5D adds the first local query RPC entrypoint and a separately tested API adapter seam under [ADR 0023](../decisions/0023-local-query-rpc-and-bookmark-continuity.md): a named two-method `WorkerEntrypoint`, `first-primary` head selection, live-only D1 bookmark continuation, and the existing canonical provider exact-name tier. It does not configure or prove an API-to-query service binding. Its boundary and evidence are recorded in [`phase-5d-local-query-rpc.md`](phase-5d-local-query-rpc.md). The public API request handler remains unchanged and unavailable. At Phase 5D close, complete search tiers/filters, the merged public cursor tuple, empty-collection semantic degradation, dedicated search limiting, approved production ceilings, remote resource IDs, deployment, and all release gates were explicit blockers. Traceability statuses remain unchanged.

Phase 5E resolves only the empty-collection representation blocker under [ADR 0024](../decisions/0024-search-collection-semantic-degradation.md): required authoritative `SearchCollection.meta.semantic_degraded`, an identical required `/v1` result mirror, four defined fallback states, no default, bounded future-value tolerance, the published recursive client-compatibility rule, and a validating empty-response example. Its contract boundary and non-claims are recorded in [`phase-5e-search-collection-degradation.md`](phase-5e-search-collection-degradation.md). OpenAPI remains prelaunch `1.0.0`, the public request handler remains unchanged and unavailable, and no PRD amendment is made. Complete search composition, provider-only applicability/`not_applicable`, the merged public cursor tuple, dedicated search limiting, approved production ceilings, remote resource IDs, deployment, and all release gates remain blockers. Traceability statuses remain unchanged.

Phase 5F addresses the false-negative trust gap in canonical model/variant exact-name classification under [ADR 0025](../decisions/0025-trusted-model-variant-name-projection.md). The implemented runtime-neutral `model-variant-name@1` core derives the complete known-name subset from a nominal immutable manifest and exact closure-bound canonical resource bytes, fully validates every model/variant resource, preserves current Model/Variant U+0000 semantics, applies pinned normalization, and binds a deterministic inventory root. Its boundary and acceptance evidence are recorded in [`phase-5f-model-variant-name-projection-core.md`](phase-5f-model-variant-name-projection-core.md). It adds no migration, index, readiness proof, writer, reader, RPC, public route, or deployment. Durable U+0000-safe persistence and the bookmark-continuous exact tier are Phase 5G follow-up work. All traceability statuses remain `Planned`.

Phase 5G is split by [ADR 0026](../decisions/0026-blob-model-variant-exact-search-cutover.md) so the publication-format cutover cannot be confused with query integration. [Phase 5G-A1](phase-5g-a1-model-variant-durable-proof-core.md) implements the runtime-neutral frozen UTF-8 byte rows, closure/revision-bound staging projection, and six-field storage-artifact proof only. [Phase 5G-A2](phase-5g-a2-model-variant-search-cutover.md) now locally implements the `STRICT` BLOB table/index, fail-before-D1 bounded lowercase-hex/`unhex` writer, forced-index runtime probe plus pinned-workerd semantic gold, schema `1.6.0`, seven-field v3 readiness/switch family, seal completeness, backup exclusion, and local canonical-source restore-rebuild seam as one atomic boundary. The local seam explicitly is not the release-gated exporter/importer or a `BE-010`–`BE-012` claim. [Phase 5G-B](phase-5g-b-model-variant-exact-reader.md) now locally implements the bounded reader, canonical rehydration, default status, neutral tier-local ordering, bookmark-continuous named RPC, internal API adapter, and acceptance boundaries without creating a public route or cursor. No remote migration, service binding, provisioning, or deployment is authorized by A1, A2, or B; every mapped traceability row remains `Planned`, while public multi-tier composition, remote/operational restore, deployment, and release acceptance remain pending.

Phase 5H is split by [ADR 0027](../decisions/0027-trusted-provider-model-id-projection.md), [ADR 0028](../decisions/0028-provider-model-id-durable-storage-cutover.md), and [ADR 0029](../decisions/0029-provider-model-id-exact-reader.md). [Phase 5H-A1](phase-5h-a1-provider-model-id-projection.md), [Phase 5H-A2](phase-5h-a2-provider-model-id-storage.md), and [Phase 5H-B1](phase-5h-b1-provider-model-id-reader.md) are locally implemented: the complete nominal projection now has schema `1.7.0` dual-BLOB storage, bounded atomic staging, forced raw and normalized index proof, v4 readiness and switch evidence, legacy-version rejection, backup exclusion, local restore-rebuild integration, literal raw-first then nonempty-normalized equality within the existing 200-byte NFC-trimmed ceiling, exact active/non-stale Offering witnesses and active targets, canonical target mapping, same-witness record-type/provider filters, deduplication and neutral tier-local order, and a first-page-only internal RPC/API seam. No remote resource, deployment, operational restore, or release evidence is claimed. Phase 5H-B2 owns multi-tier exact composition and the authenticated merged public cursor; prefix/keyword, semantic, and public route integration remain later work. All traceability statuses remain unchanged.

[ADR 0030](../decisions/0030-composed-exact-search-and-compact-cursor.md) and [Phase 5H-B2](phase-5h-b2-composed-exact-search.md) now define the next local boundary: one bookmark-continuous composed exact RPC, deterministic higher-tier winner suppression, a complete compact class/stable-ID authenticated cursor, record-type-only filter scope, and explicit provider-only semantic inapplicability. Retained-hot publication resolution across multiple switches, complete filters, later search tiers, public Request/Response wiring, remote resources, and deployment remain later release blockers. Traceability stays unchanged.

[ADR 0031](../decisions/0031-retained-hot-publication-continuity.md) and [Phase 5I-A](phase-5i-a-retained-hot-publication-continuity.md) now locally implement the retained-hot boundary. Serving schema `1.8.0` adds two partial covering indexes over the append-only active-departure and prior-rollback-candidate-replacement history references with semantic history-guard preflight. Resolver V2 takes an API-computed availability horizon, and merged-read V2 carries that horizon through every exact-reader sentinel. Fresh work requires `now + 15 minutes`; a continuation requires its original authenticated expiry; a historical cutoff must be strictly later than that horizon plus 30 seconds. Nonnegative/future-reference guards and subtraction-form cutoff arithmetic fail closed without changing retention policy. Active/current rollback remain eligible, resolver V1 remains compatibility-only, and no mutable ledger or extra drain window is introduced. Public routing, semantic namespace retention, physical pruning, remote resources, operational traffic/lifecycle chaos, and deployment remain pending; all traceability statuses stay unchanged.

[ADR 0032](../decisions/0032-local-named-query-service-binding.md) and [Phase 5I-B](phase-5i-b-local-named-query-service-binding.md) now configure and prove the local API-to-query Worker boundary. The API receives only a `CATALOG_QUERY` binding to the named `CatalogQueryService` entrypoint and still has no D1 binding. The API workerd suite dry-run-compiles the actual query Worker and calls it as an auxiliary Worker over JSRPC; no handwritten service mock or HTTP compatibility route is used. The public handler remains unchanged and `/v1/search` remains closed. Distinct production/preview names, remote resources, complete search composition, public conformance, deployment, and release evidence remain pending; all traceability statuses stay unchanged.

[ADR 0033](../decisions/0033-provider-eligibility-filtering.md) and [Phase 5J](phase-5j-provider-eligibility-filter.md) add the first complete structured provider filter to composed exact Model/Variant search. Serving schema `1.9.0` adds a forced provider/target/Offering eligibility index over the complete closure-bound provider-model-ID projection. One canonical provider stable ID changes target membership only, is independently satisfied by any active non-stale Offering from that provider, applies identically to canonical-name and provider-model-ID candidates before limit, skips Provider-name results, preserves canonical facts and neutral stable-ID order, and is authenticated by the existing cursor. The public handler remains closed; remaining filters, later search classes, remote resources, deployment, and release evidence remain pending, and traceability statuses do not advance.

[ADR 0034](../decisions/0034-canonical-family-filtering.md) and locally implemented [Phase 5K](phase-5k-canonical-family-filter.md) define the schema-neutral local boundary: one canonical family stable ID filters both exact Model/Variant target tiers through their required canonical `family_id`, composes conjunctively with provider eligibility and compatible record type, runs before limit and winner suppression, and skips Provider-name results. Existing exact indexes, schema `1.9.0`, statement ceilings, canonical facts, neutral order, and the authenticated cursor tuple remain unchanged. Public routing, bidirectional ModelFamily publication closure, the separate model filter, offering-derived filters, later search classes, remote resources, deployment, and release evidence remain pending; traceability statuses do not advance.

[ADR 0035](../decisions/0035-canonical-family-model-variant-publication-closure.md) and locally implemented [Phase 5L](phase-5l-canonical-publication-relationship-closure.md) own that publication-closure follow-up. The implementation validates at most 100,000 relevant resources and 100,000 ModelFamily membership edges in `O(F + M + V + E)` time, after persisted hashes and descriptor manifest closure succeed but before the content-aware builder returns trust. It requires exact ModelFamily/Model agreement and Variant/Model family agreement for every lifecycle state, treats empty families as valid and `model_ids` as an unordered set, and derives reverse Model-to-Variant navigation from the complete Variant inventory. This pre-release hard cutover changes no D1 schema, proof version, descriptor-only manifest, SQL, public route, or traceability status. Full `DATA-003` and `RULE-003` presentation acceptance remains pending.

[ADR 0036](../decisions/0036-publication-pinned-dataset-metadata.md) and locally implemented [Phase 5M](phase-5m-public-dataset-metadata.md) define the next vertical slice: only `GET|HEAD|OPTIONS /v1/metadata` replaces its local `503` stub. The storage-free API resolves active or exact retained-hot publication state through V2, then calls one fixed SELECT-only metadata operation over the existing named query binding. Publication builds materialize the canonical active/non-stale counts and provider-slice flags in an immutable, seal-bound summary; the query reads it by primary key, verifies every authority field and its domain-separated hash, and performs no request-time canonical-resource or provider-slice scan. The response uses first activation time, deterministic provider-slice notices, separate normalization/display-order versions, a Monday/Thursday UTC refresh window, strong exact-byte ETags, and `private, no-store`. Every other public data route, remote resource, public-domain origin, deployment, and release gate remains closed; traceability does not advance from local implementation alone.

[ADR 0037](../decisions/0037-stale-offering-eligibility-filtering.md) and locally implemented [Phase 5N](phase-5n-stale-offering-filter.md) add the next exact-search filter boundary. An absent stale filter preserves current behavior; explicit `stale=true|false` selects known-active Offerings with that exact publication-time state, and a combined provider/stale filter must be satisfied by the same target-eligibility Offering. The provider-model-ID matching Offering remains independent but adopts the selected stale boolean so stale exact IDs are discoverable only when requested. Schema `1.11.0` adds one target-first eligibility index while preserving fixed SELECTs, the four-statement ceiling, cursor version and order, canonical card facts, privacy, and the closed public search route. Local implementation and verification are complete; remote migration, public routing and conformance, deployment, and release evidence remain pending, so traceability stays `Planned`.

[ADR 0038](../decisions/0038-publication-pinned-model-detail-read-seam.md) and locally implemented [Phase 5O-A](phase-5o-a-model-detail-read-seam.md) add the next internal API/query boundary. One exact lowercase Model stable ID selects the unchanged canonical Model from an active or exact retained-hot publication through resolver V2, its bookmark, the identical horizon, and one fixed SELECT-only publication-resource lookup. The reader verifies the closure sentinel, complete Model contract, exact identity/type/publication, byte bounds, and recomputed resource hash; the API adapter validates the existing `ModelDetail` envelope as hostile output. Schema remains `1.11.0`, and unit, named-binding, and pinned-workerd tests pass. This phase does not change the public handler, use Cache API, create ETags, configure remote resources, migrate, provision, deploy, or open `/v1/models/{model_id_or_slug}`. Current and historical slug authority plus public response-size/load admission remain later prerequisites, so traceability stays `Planned`.

[ADR 0039](../decisions/0039-publication-model-slug-projection-core.md) and locally implemented [Phase 5O-B1](phase-5o-b1-model-slug-projection-core.md) add a pure `model-slug@1` projection before serving storage. It derives the only publication boundary from trusted `manifest.generatedAt`, requires exact known current slugs, validates caller-supplied canonical history intervals, permanently reserves every begun slug within the Model route namespace, rejects multi-Model collisions, and hashes both the complete supplied source history and its deduplicated current/historical mapping. Conservative 16 MiB retained-resource and 8 MiB per-inventory encoded ceilings are allocation guards; accepted-bound workerd/load evidence remains pending. B1 remains schema-neutral and unrouted because it cannot authenticate caller snapshot completeness. B2 must add canonical extraction or an archived authoritative input and serving `1.12.0` storage/readiness/switch/backup/restore; B3 owns RPC, cache, HTTP, and redirect-versus-read semantics. No traceability status advances: `DATA-001` remains `Implemented`, and every affected pending row remains `Planned`.

[ADR 0040](../decisions/0040-canonical-model-slug-history-capture.md) splits that B2 follow-up into explicit saga stages. [Phase 5O-B2A](phase-5o-b2a-canonical-model-slug-capture.md) hardens Model-only canonical history with exact capability marker `model-slug-history-guard@1` and defines one fixed `first-primary` acquisition of exactly the trusted manifest Model IDs while the same canonical writer drain remains held from manifest/resource assembly through acquisition. Its candidate returns boundary-adjusted rows, same-statement canonical current slugs, both B1 roots/counts/mappings, and a private bookmark, but cannot gate readiness. B2B must write/read-verify a content-addressed private R2 artifact and stage serving schema `1.12.0`; B2C must bind lifecycle proofs, backup/restore, and an internal indexed lookup to that artifact. The handoff is never a cross-D1 transaction. B2A leaves serving `1.11.0`, all public routes, remote resources, and deployment closed, and no traceability status advances until implementation and complete acceptance evidence exist.

[ADR 0041](../decisions/0041-model-slug-sidecar-archive-and-staging.md) and [Phase 5O-B2B](phase-5o-b2b-model-slug-archive-staging.md) define that durable handoff. A canonical `model-slug-history-artifact@1` sidecar binds the exact base publication bundle to the same-statement current-slug census, boundary-adjusted history, and both B1 roots/counts without duplicating base Model resources or derived mappings. The pipeline uses a computed-digest-only R2 key, conditional create-only write, bounded hostile readback, independent digest verification, and a full projector replay against the separately trusted base bundle before minting nominal archive authority. Serving migration 0015 advances clean `1.11.0` to dormant `1.12.0` tables for the artifact proof and exact indexed mapping; bounded idempotent staging cannot seal, mark ready, switch, or serve. B2C will advance to `1.13.0` for lifecycle, backup/restore, and internal read authority. Remote private bucket lock, provisioning, deployment, and every public route remain closed.

[ADR 0042](../decisions/0042-model-slug-lifecycle-authority.md) and locally implemented [Phase 5O-B2C-A](phase-5o-b2c-a-model-slug-lifecycle.md) provide the first part of that cutover. Pristine migration 0016 advances dormant schema `1.12.0` to `1.13.0`, exact-inventories the retained schema and pinned runtime control tables, adds the partial unique current-Model slug index, makes exact staged sidecar proof a closure-seal prerequisite, and replaces the cumulative v4 lifecycle family with archive-bound readiness/switch v5. Activation requires an unexpired v5 attestation; rollback requires a fresh read-only hostile R2 verification of the immediate retained target plus exact serving/index parity. Backup v1 rejects schema `1.13.0`; proposed ADR 0045 would require a cumulative schema-`1.14.0` lifecycle-v6 embedding-artifact cutover before public routing after product-owner approval. B2C-C owns the unrouted stable-ID/current-slug/historical-slug internal lookup, and B3 owns HTTP/cache semantics. The full local repository gate passes, including migration, archive, staging, readiness, switch, hash, backup, privacy, type, worker-runtime, build, browser, accessibility, supply-chain, and production-mode evidence. Remote resources, deployment, and traceability advancement remain pending.

[ADR 0043](../decisions/0043-byte-authentic-publication-recovery.md), [proposed ADR 0045](../decisions/0045-publication-bound-embedding-recovery.md), and [Phase 5O-B2C-B](phase-5o-b2c-b-portable-recovery.md) split portable recovery at the base-byte and proposed embedding-output authority boundaries. B2C-B1 implements `publication-recovery-base@1`: fixed canonical source rows, nominal manifest authority before writes, bounded content-addressed chunks, conditional create-only private storage, exact-key get-only hostile verification, and independent semantic closure replay. Its pinned-workerd joint row/byte profile repeats an identical archive and full replay for exactly 50,000 contract-valid rows and 25,148,376 bytes including the root; the production source-byte admission estimate is 84,267,088 bytes under the 96 MiB budget, while the stricter including-root test estimate is 84,285,360 bytes. The exercised inventory is exactly 18 objects, and unit evidence covers the 1,024/1,025 stream-chunk boundary. A separate independent canonical packer proves the verifier's object ceiling: pinned workerd R2 replays exactly 63 source objects plus root, while the unit hostile-R2 boundary rejects 64 source objects plus root after the root read and before source reads. Product-owner acceptance of ADR 0045 is required before B2C-B2 may implement the dormant `publication-embedding-artifact@1` codec/writer/verifier, schema `1.14.0` lifecycle v6, protected three-artifact `backup-v3@1`, or isolated `serving-restore-rebuild@6`. Current-alias incompatibility would keep semantic search disabled after recovery; it never changes exact/structured/detail availability. B2C-B3 remains canonical/evidence recovery, offline migration-away, protected access auditing, remote D1/R2/Vectorize exercises, and measured RPO/RTO. No local evidence advances release traceability.

[ADR 0042](../decisions/0042-model-slug-lifecycle-authority.md) and locally implemented [Phase 5O-B2C-C](phase-5o-b2c-c-model-slug-internal-read.md) complete the independent internal-read slice. The additive V2 operation distinguishes exact stable IDs from strict slugs, continues from one resolver bookmark and horizon, and performs one bounded SELECT-only schema-`1.13.0` read that requires closure/artifact authority, both exact/current named indexes, resource-content equality, and canonical current-slug parity. It returns the unchanged canonical Model plus a closed stable/current/historical provenance union without returning a historical submitted slug. Unit and actual-D1 workerd evidence cover active, rollback, displaced retained-hot, strict-cutoff, hostile-output, mapping/proof/canonical corruption, index plans, V1 compatibility, named binding, and unchanged privacy/public-route boundaries. B3 still owns HTTP, redirect/direct response, cache, CORS, ETag, response admission, and public conformance. Local evidence cannot advance remote, deployment, or release traceability.

[ADR 0044](../decisions/0044-public-model-detail-http-cache.md) and [Phase 5O-B3](phase-5o-b3-model-detail-http-cache.md) fix that implementation contract: stable IDs, current slugs, and explicitly pinned historical slugs produce the same canonical `200`; unpinned historical slugs issue a bodyless relative `308` to the verified stable ID. The locally implemented B3-A increments add one shared exact-byte encoder that fails closed above 65,536 bytes, a hostile API V2 adapter that distinguishes stable, current-slug, and historical-slug authority without serializing lookup provenance, publication-wide admission in the authorized V5 activation/rollback path, and a bounded fail-closed pre-open audit of active, rollback, and the complete literal retained-hot set. Exact 65,536/65,537 vectors, strict identifier boundaries, byte identity, V1 compatibility, hostile RPC/D1 cases, aggregate audit ceilings, head drift, and an actual A-to-B-to-C lifecycle pass locally. The closed B3-B HTTP/cache composition and exact generated Model-detail OpenAPI/checker also pass locally without opening the route. The guard is explicitly an authorized-application-path boundary rather than a false D1-level non-bypass claim, and a callable audit is not protected release evidence. Protected audit invocation/evidence, recovery admission, public routing, remote/load/abuse tests, production configuration, deployment, and release acceptance remain pending; no traceability status advances from local evidence.

[ADR 0049](../decisions/0049-public-methodology-detail.md) and [Phase 5Q](phase-5q-public-methodology-detail.md) accept a bounded local/test methodology-detail slice without depending on Model-route recovery. One shared immutable registry supplies exact historical version/effective-time/path metadata to both dataset metadata and the existing `MethodologyDetail` encoder. Complete request/header validation and registry/environment closure form a withheld plan before limiting: unregistered GET/HEAD/OPTIONS and every preview/production methodology request release fixed `404` after the limiter with no resolver or query RPC. Registered local/test OPTIONS releases fixed preflight; registered local/test GET/HEAD then use resolver V2 followed by one dedicated bookmark-continuous SELECT-only `readMethodologyContextV1` for selected publication/schema plus protected origin. The closed context envelope validates the already-registered version without using it as a SQL bind. Every methodology response is `private, no-store`; successful responses retain the generated methodology-specific publication `Vary`, and Cache API is absent. [Phase 5T](phase-5t-local-methodology-acceptance.md) separately implements the local semantic methodology, stable historical page, material-change log, and browser evidence for `FE-050`–`FE-052`. Remote conformance, deployed accessibility and historical-retention acceptance, deployment, and release evidence remain pending; no traceability row advances from local implementation.

[ADR 0050](../decisions/0050-gdpr-accountability-readiness.md) and [Phase 5R](phase-5r-gdpr-accountability-readiness.md) implement a pending-only accountability prerequisite without making a legal determination. The closed `config/gdpr-accountability.json` manifest fixes the six `GATE-gdpr-accountability` requirement IDs, hash-binds the working notice and three public-safe drafts, preserves exact draft/pending evidence categories with null approval metadata, and requires release, gate, and compliance authority to remain false. `npm run gdpr-accountability:check` validates strict bounded JSON, artifact integrity and blocker markers, `Planned` trace status, and continued predeployment/preview embargo; it is part of `verify` and its root script authority is pinned by the predeployment policy. This local check is distinct from technical zero-visitor-data verification and cannot substitute for the authorized owner packet or deployed-notice hash. All remote, legal-owner, deployment, and release evidence remains pending.

[ADR 0052](../decisions/0052-publication-pinned-frontend-model-detail.md) and locally implemented [Phase 5U](phase-5u-publication-pinned-model-facts.md) add one exact signed query-free Model-detail `GET`, mandatory pin to the middleware-selected metadata publication, authentication before any API source-address capability, and a shared post-admission ADR 0044 executor. The bounded SSR client admits only exact status/header/canonical-byte/ETag/contract/publication matrices; stable IDs and current slugs render canonical Model Facts, and validated historical slugs issue a bodyless stable-ID frontend `308`. Local unit, native-workerd, and three-Worker browser evidence passes while the public Model API and preview/production Model routes remain closed. No provider comparison, Variant page, remote configuration, recovery claim, deployment, or traceability advancement is included.

[ADR 0053](../decisions/0053-publication-pinned-exact-model-discovery.md) and locally implemented [Phase 5V](phase-5v-exact-model-discovery.md) add the next local-only signed frontend read: canonical NFC-trimmed exact Model queries use fixed `record_type=model`, limit 20, optional authenticated cursor, and an explicit validated publication pin. First pages use metadata-selected publication; continuation URLs pair cursor with its old publication so retained pagination survives a new head and expired retention fails unavailable. API authentication precedes all source-address, limiter, resolver, cache, and query capabilities; the existing merged exact-search operation and a separate local-only cursor key remain authoritative. SSR visibly identifies the results publication and renders only an “Exact Model matches” identity list linked to stable-ID Model Facts because the current `SearchResult` is not the complete evidence-backed Model-card projection. Local unit, native-workerd, and three-Worker browser evidence passes. Public search, test/preview/production ingress, aliases, prefix/keyword, semantic retrieval, full cards, remote configuration, deployment, and traceability advancement remain closed.

Accepted [ADR 0054](../decisions/0054-canonical-model-card-projection.md) and locally implemented [Phase 5W](phase-5w-exact-model-cards.md) add the Model-only card follow-up. The exact-name and provider-model-ID tiers project from the complete canonical Model they already read and verify before discarding it, so a dedicated bookmark-continuous compact-card operation adds zero SQL and no per-result detail call. A closed `ModelCard` projection contains only required canonical Facts plus the allowed cataloged-provider count; match provenance stays outside the card in the exact `{match_kind, model}` wire item, and provider/stale/affiliate witnesses cannot alter model projection bytes or order. One shared encoder fixes metadata resource `exact_model_cards`, exact byte order, and a 65,536-byte whole-page ceiling. Variant cards, optional compact fields, public/remote ingress, publication-time worst-page admission, deployment, and traceability advancement remain closed.

Accepted [ADR 0055](../decisions/0055-publication-pinned-exact-variant-cards.md) and locally implemented [Phase 5X](phase-5x-exact-variant-cards.md) define the explicit-Variant follow-up. A separate card projection copies only verified canonical Variant facts plus structural Model/family IDs, adds no SQL, and cannot inherit from the canonical Model or accept provider-derived content beyond the cataloged-provider count. The purpose-separated signed local `/variants` journey links to existing canonical Model Facts without inventing Variant detail authority. Provider comparison remains sequenced behind complete Offering/Provider/target/Price/PrecisionObservation/EvidenceSummary closure and deterministic current-comparison policy. Public/remote ingress, deployment, and traceability advancement remain closed.

Accepted [ADR 0056](../decisions/0056-offering-relationship-closure.md) and locally implemented [Phase 5Y-A](phase-5y-a-offering-relationship-closure.md) define the first provider-comparison prerequisite without selecting or presenting any comparison facts. The common persisted-content builder hard-fails before trust when bounded Provider, Offering, Model-or-Variant target, Price, PrecisionObservation, and EvidenceSummary resources disagree on identity, provider attribution, exact forward/reverse child sets, the five-field Offering applicability tuple, evidence existence/subject ownership, or required time ordering. Relevant resource count and UTF-8 bytes are admitted before parsing, and relationship edges are incrementally bounded. Evidence field/value equivalence, current-value selection, comparison/display/sort policy, API/UI, source access, remote configuration, deployment, and traceability advancement remain pending.

Accepted [ADR 0057](../decisions/0057-selection-free-offering-observation-set.md) and locally implemented [Phase 5Y-B](phase-5y-b-offering-observation-set.md) define the lossless input to later current-value policy. From a trusted manifest reconciled to the complete exact persisted resource bytes, one versioned internal projection retains every Offering Price, PrecisionObservation, and applicable EvidenceSummary, copies the exact Provider display-name Fact, extends evidence-subject closure to that Provider Fact, and commits fixed aggregate membership counts, per-set hashes, and an ordered inventory hash. It has no clock, latest/current choice, overlap resolution, precedence, rank, comparison order, API, or UI. Evidence field/value semantics, current-value policy, comparison transport/presentation, source access, remote configuration, deployment, and traceability advancement remain pending.

Accepted [ADR 0063](../decisions/0063-current-offering-observation-authority.md) and [Phase 5Y-C](phase-5y-c-current-offering-observation-authority.md) record the next comparison prerequisite discovered by implementation and independent review. EvidenceSummary source labels cannot prove Provider source ownership, approved source-register membership, verification, or precedence. Before current Price/Precision selection, a successor publication-format cutover must derive and persist a bounded manifest-bound authority artifact from fenced canonical claims, exact scopes, observations/evidence, source registers, verification receipts, and field-specific policy, then extend closure/readiness/backup/restore proofs. The rejected prototype is absent; comparison rows, Offering Facts, API/UI, remote configuration, and deployment remain pending. No traceability status advances.

Accepted sequencing [ADR 0064](../decisions/0064-sequence-fenced-claim-authority-cutover.md) and [Phase 5Y-D](phase-5y-d-fenced-claim-authority-sequence.md) fix the safe implementation order after a canonical audit found four missing authorities: typed field-policy order, immutable verification receipts, reproducible approved endpoint identity, and complete claim-to-public-field commitments. Migration 0006 installs an environment-initialization-gated legacy freeze; a fence-bound provenance-v2 schema may install dormant, while first authority admission, source execution, and trusted minting require proven initialization, quiescence, active guards, and current fence. The later closure/readiness/recovery cutover must be one cumulative generation coordinated with proposed, owner-pending ADR 0045; no implementation or traceability status advances here.

[ADR 0065](../decisions/0065-fenced-provenance-v2-authority.md) and [Phase 5Y-E](phase-5y-e-fenced-provenance-v2-authority.md) approve the schema/writer shape. Implement it in dormant-to-activation slices: exact predecessor plus blocked authority-plan/bundle shapes; protected endpoint/policy/adapter registration and only then plan approval/bundle opening; fenced claim and verification chunks; exhaustive eligibility/candidate-field closure without current selection; then bounded acquisition permits and sealed-source activation. Prerequisite slices leave `publication_roster_outcome_source_execution_blocked` in place. No current requirement status advances until executable evidence exists.

ADR 0006's `PublicationWorkflow` remains a separate deferred Phase 3/7 prerequisite. The predeployment embargo intentionally forbids Workflow and data bindings in tracked local Wrangler configuration, while the inert preview proposal keeps the reserved Workflow unprovisioned with a null schedule. No frontend slice weakens that boundary; a successor owner-authorized platform design must add protected bindings and runtime evidence.

1. Implement the non-routable SELECT-only query Worker and storage-free public API Worker with typed service-binding operations.
2. Implement every collection/detail route, `Fact<T>` provenance, metadata/next-refresh, exact decimals, filters/sorts, authenticated cursors, CORS, ETags, publication pinning, stable errors, and OpenAPI examples.
3. Enforce method/query/filter/cursor/result/response/CPU/subrequest/semantic-call bounds and rate-limit every request before head/cache resolution.
4. Disable automatic invocation logs/traces, Tail/Logpush export, Analytics Engine request events, Web Analytics, and custom telemetry Worker-wide; enforce no retained request ID, no public query embedding until processor approval, no visitor-derived cache keys, and validate seeded privacy canaries against every allowed sink.

Exit evidence: API conformance, abuse/load, cache/version, privacy, and no-mutation penetration suites pass; `GATE-api-contract`, `GATE-api-abuse`, and API portions of `GATE-cost-fail-safe` pass.

## Phase 6 — accessible model-first web product

1. Build Astro SSR/static surfaces for home/search, model/variant/family/provider/detail, Model Facts, Offering Facts, methodology/history/change log, API docs, privacy/legal/non-affiliation/API/dataset terms, sitemap, and robots.
2. Keep model cards limited to model facts plus the distinct active non-stale provider count; pass provider eligibility separately and never provider-rank cards.
3. Implement neutral offering tables with explicit currency scopes, URL state, progressive enhancement, unknown/stale/inactive states, and no recommendation/winner language.
4. Apply SSR/static security headers with no third-party analytics/connect origins, global `no-referrer`, preview `noindex`, zero visitor telemetry/browser storage, safe source rendering, and direct static referral-link isolation/disclosures when that feature is later enabled.
5. Test raw HTML without JavaScript, keyboard/focus/screen readers, 320-pixel layout, 200%/400% zoom, contrast/reduced motion, and versioned performance profiles.

Exit evidence: primary journeys pass the preview-applicable inputs of `GATE-manual-a11y`, `GATE-performance`, and `GATE-neutrality-invariance`. Preview rehearsals exercise `GATE-zero-visitor-data`, `GATE-referral-zero-tracking`, `GATE-gdpr-accountability`, and `GATE-environment-isolation`, but production/legal-owner inputs remain pending until Phase 9.

## Phase 7 — CI/CD, operations, and first preview candidate

[Phase 5P-A](phase-5p-a-predeployment-embargo.md) implements the deny-by-default repository prerequisite to this phase: independently discovered package, Wrangler, workflow, and local-action files plus npm lifecycle policy/lock inputs must match a closed non-secret inventory and reviewed content digests. CI validates them outside npm hooks before strictly allowlisted dependency rebuilds; `verify` repeats the check, keeps every environment unprovisioned, allows only approved telemetry-disabled Wrangler dry runs, and rejects authority/configuration drift before project-defined build or test scripts. It creates no deployment authority, branch protection, or remote evidence and advances no traceability status.

[Phase 5P-B](phase-5p-b-inert-preview-topology.md) and [proposed ADR 0046](../decisions/0046-inert-preview-topology-and-split-authority.md) add a code-owned, exact, inert preview proposal: a dedicated account is required; four Worker, two D1, two private R2, one Vectorize, one Workflow, and four limiter namespace names are reserved; public capability and observability boundaries are closed; and seven future identities/GitHub environments separate planning, bootstrap, deployment, migration, probes, Worker rollback, and publication rollback. Every authority flag remains false and every remote identifier, jurisdiction/location, host, route, schedule, credential/effective permission, selected smoke mechanism, and created-state remains absent. The checker rejects duplicate JSON keys, additive authority, and topology drift, but no Wrangler overlay, protected environment, identity, resource, route, legal/spending approval, or deployed evidence exists, so this increment advances no traceability status.

[Phase 5P-C](phase-5p-c-api-environment-continuity.md) and [ADR 0047](../decisions/0047-api-query-environment-continuity.md) close the API's source-code `local` fallback. The storage-free API accepts one exact environment-owned binding, settles the hardened transient public-read limiter, fails closed on invalid configuration, and forwards the same value through resolver and read envelopes. Exact local Wrangler policy, generated types, hostile unit cases, and actual multi-Worker local/mismatch behavior are tested. The narrow local preview-plan plumbing gate is replaced by an explicit protected preview configuration and remote mismatch-probe gate; account/binding isolation, protected authority, smoke, resource, route, deployment, and release evidence remain pending, and no traceability status advances.

[Phase 5P-D](phase-5p-d-unrouted-model-detail-runtime.md) and [ADR 0048](../decisions/0048-unrouted-model-detail-protected-runtime.md) assemble the already closed Model-detail HTTP/cache composition from exact local `DEPLOYMENT_ENV=local`, `PUBLIC_API_ORIGIN=https://api.example.test`, and `API_TRANSPORT_POLICY=local_test` bindings plus the existing named query service, transient limiters, Cache API, Web Crypto, execution-context scheduler, and clock. The exact tuple is configuration- and type-checked, cannot be derived from visitor host material, and is hostile-tested through the internal Worker-runtime seam. The live API export remains unchanged and Model routes remain closed. Preview and production origin/transport values, protected remote conformance, recovery admission, routing, load evidence, deployment, and release acceptance remain pending; no traceability status advances.

The first Phase 7.1 increment adds the versioned closed [`config/verification-artifacts.json`](../../config/verification-artifacts.json) registry and a pure/CLI validation gate. Version 1 is intentionally limited to the current `Implemented` rows and their local test-source anchors: code owns the exact 14 primary-ID/source-ID/path/criterion mappings, and the registry must mirror that authority byte-for-structure. The gate fully parses matrix-shaped rows, rejects missing, malformed, orphan, duplicate, relabeled, or unanchored mappings, and does not require entries for `Planned` work. Any `Designed`, `Verified`, `Accepted`, or `Released` row fails closed until a successor schema is designed for those evidence authorities. This bookkeeping guard creates no execution result, remote evidence, approval, or status advancement.

[ADR 0058](../decisions/0058-dormant-unbound-publication-workflow-planner.md) and locally implemented [Phase 7.2-A](phase-7-2-a-dormant-publication-workflow-planner.md) add the real `PublicationWorkflow` class without adding a Workflow binding or schedule. The generated production `CloudflareEnv` remains empty. An exact preview-shaped Monday/Thursday event derives only the stable `provider-refresh-v1` occurrence identity and returns one sub-4-KiB dormant plan through one pure step; manual input, payload authority, or malformed platform metadata fails closed. The preview-only resource name remains separate from the environment-neutral logical schedule identity. Focused unit and direct-workerd tests plus a Wrangler dry-run prove local class/config compatibility and no bindings, not native Workflow persistence, retry, scheduling, or restart. Run-plan/provider scope, D1 ledger transactions, child Workflows, status mapping, protected configuration, remote resources, deployment, and every traceability advancement remain pending.

Accepted [ADR 0059](../decisions/0059-normalized-publication-run-plan-authority.md) and locally implemented [Phase 7.2-B](phase-7-2-b-publication-run-plan-authority.md) add the first local run-plan/provider-scope authority. Additive canonical migration `0005_publication_run_plan_authority.sql` normalizes exact immutable plan, Provider, policy, seal, approval, revocation, and capability records, and independently freezes referenced roster, roster-item, and source-compliance authority rows at plan membership. Plans bind one preview or production environment, the stable schedule and half-open interval, 1–16 sorted Provider/version entries with six typed ceilings, the exact three policy roles, and exact product/legal/platform approval of one static artifact. The fixed read-only resolver requires exact ID/hash, protected expected environment and runtime versions, and canonical scheduled instant; it recomputes provider-scope, policy-set, and root hashes, validates source approval at that instant, and never infers latest state. Focused SQLite, unit, and actual workerd/D1 verification passes. No real plan, run write, Workflow/configuration change, protected binding, preview/production migration, deployment authority, or traceability advancement exists. Missing-plan admission, attempt/replay, terminality, deadline, same-provider concurrency, report completeness, production schedule/name hardening, and ambiguous-write reconciliation remain coupled inputs to the next ADR.

Accepted [ADR 0060](../decisions/0060-publication-run-admission-and-terminal-coordination.md) and [Phase 7.2-C](phase-7-2-c-publication-run-admission-and-terminal-coordination.md) close those inputs with runtime-neutral oracles and hostile tests only. The slice distinguishes rejection from a run, engine retry from protected adjacent replay, and run outcome from publication disposition; anchors the sealed deadline to scheduled time; requires cross-occurrence fenced Provider ownership; defines exact response-loss reconciliation; and emits a bounded closed report. Phase 7.2-D must add and independently review the canonical migration plus fixed D1 adapter, including non-source operational terminal evidence. Phase 7.2-E may then add protected bindings and native Workflow evidence. No run, migration execution, binding, source call, remote resource, or deployment is authorized here.

Accepted [ADR 0061](../decisions/0061-additive-publication-orchestration-ledger.md) and locally implemented [Phase 7.2-D](phase-7-2-d-publication-orchestration-ledger.md) implement the safe local foundation as a parallel coordination ledger. The legacy pipeline/provider/evidence graph is not shadow-written: environment initialization freezes new legacy writes, while immutable coordination rows own attempt-1 admission, exact plan Provider scope, budget reservation, fenced claim/reconciliation/release, source-free operational roster outcomes, Provider terminals, and sealed run reports. Rejection and replay persistence fail closed until atomic D1 admission and protected replay resolvers exist; retained-head authority and all source execution remain blocked on their fixed resolver and provenance v2. The corrected Phase C contract uses `prn_` for selected Provider slices. Phase 7.2-E additionally remains blocked on protected bindings and global budget authority. No remote migration, binding, resource, or deployment is authorized here.

Accepted [ADR 0062](../decisions/0062-admission-authority-monotonicity.md) and locally implemented [Phase 7.2-D1](phase-7-2-d1-admission-authority-hardening.md) close two prerequisites found during Phase 7.2-E design review: exact absent-ID versus invalid-identity classification, and monotone revocation truth for already admitted scheduled occurrences. Migration 0007 is local only and leaves the admission-rejection activation blocker intact. Full atomic reason derivation, precedence, protected runtime authority, bindings, native Workflow evidence, remote migration, and deployment remain later work.

1. Make every automated trace ID resolve to a test/report artifact and fail CI on missing/orphan/duplicate mappings.
2. Reconcile the already bounded preview resources only through checked-in inventory and idempotent commands; run drift detection and protect production destructive operations.
3. Add protected GitHub deployment environments, resource-scoped rotatable Cloudflare credentials, approval gates, artifact/SBOM retention, dry runs, migrations, and independent code/data rollback.
4. Exercise alerts, cost breakers, secret rotation, source drift, quarantine, erroneous mass publication, backup/restore, search rebuild, and rollback runbooks.
5. Ask independent requirements, architecture, security/privacy, data-neutrality, accessibility, and operations reviewers to audit the preview; incorporate every blocking finding.

Exit evidence: every vertical-slice composite gate whose declared inputs/environments exist passes, all production-only gates remain explicitly pending, and one reviewed preview candidate is reproducibly deployable.

## Phase 8 — four-provider launch dataset

For each remaining provider independently:

1. Obtain authorized approval of the dated legal/source register and exact expected roster.
2. Add minimal redacted fixtures and deterministic parsing before considering AI/browser use.
3. Run adapter, applicability, DLP, egress, drift, cost, and failure-isolation gates.
4. Publish only to preview, compare all neutral invariants, and keep the adapter disabled in production even when every preview roster item is terminal and evidence-backed.

Exit evidence: exactly four production-ready but production-disabled provider adapters form a reviewed preview candidate. Each has at least the default 20 active non-stale offerings or an explicit documented product-owner amendment; `GATE-provider-launch` remains pending production runs.

## Phase 9 — production observation and release

1. Obtain authorized brand/domain clearance or choose an authorized cleared replacement; obtain product/legal approval for distinct code license and dataset/API terms.
2. After explicit spending/deployment authorization, provision production from inventory, verify no preview identity can mutate it, deploy read services, then enable approved providers and run the first controlled pipeline/publication independently.
3. Keep the release under manual approval while `GATE-provider-launch` obtains current production-run evidence and Monday/Thursday production runs complete successfully for two consecutive weeks; a failed or quarantined occurrence is not success.
4. Gather the signed release-manifest inputs for all 24 `RGA-REL-AC-*` coordinators, execute final restore/rollback/search rebuild, security/privacy, accessibility, performance, cost, legal, repository, and operational checks.
5. Publish only when every release coordinator and required composite gate passes with current artifacts and no unapproved exception.

Exit evidence: the production URL/API, Cloudflare resources, GitHub settings/actions, signed release manifest, four-provider dataset, two-week run history, and monitoring prove every `REL-AC-01`–`REL-AC-24` item. Only then may the project be described as live and production-ready.

## Stop conditions

Stop the affected workstream and preserve last known good when any of these occurs:

- PRD ambiguity or semantic contradiction requiring product-owner interpretation.
- Missing/expired source permission, terms/robots prohibition, credential leak, failed SSRF/DLP canary, or unretainable evidence.
- Claim applicability conflict, unsupported fact, schema drift, or anomaly that cannot be resolved deterministically.
- Publication/search/version probe failure, backup inconsistency, restore failure, or rollback-target loss.
- Neutrality, accessibility, privacy, security, performance, cost, or environment-isolation gate failure.
- Budget breaker, provider roster shortfall, name/domain failure, or missing two-week production evidence.

These conditions block only the unsafe publication/provider/workstream where isolation is possible; they do not authorize silent omission, inference, or weakening a gate.
