{
  "name": "@quant-clarity/pipeline",
  "version": "0.0.0",
  "private": true,
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "build": "WRANGLER_SEND_METRICS=false wrangler deploy --dry-run --outdir dist",
    "cf:types": "WRANGLER_SEND_METRICS=false wrangler types --env-interface CloudflareEnv worker-configuration.d.ts",
    "cf:types:check": "WRANGLER_SEND_METRICS=false wrangler types --check --env-interface CloudflareEnv worker-configuration.d.ts"
  },
  "dependencies": {
    "@quant-clarity/api-core": "0.0.0",
    "@quant-clarity/contracts": "0.0.0",
    "@quant-clarity/domain": "0.0.0",
    "@quant-clarity/publication-core": "0.0.0"
  }
}
